import styled from "styled-components";

export const Buttons = styled.div`
	display: flex;
	flex-direction: row;
	gap: 10px;
	position: absolute;
	bottom: 0;
	left: 0;
	z-index: 1;
	background: black;
`;
