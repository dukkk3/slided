import * as model from "../../../main-page.model";

export const { $openingEnded, $animationCanBePlayed, $interactiveEnabled } = model;
export const iteration0 = model.createSpringUtilsOfFlowIteration(0);
