import { animated } from "@react-spring/web";
import styled from "styled-components";

export const LoaderGroup = styled(animated.div)`
	display: flex;
	align-items: center;
	justify-content: center;
`;
